# CMoney 形象網站原型｜設計系統指南

這份文件記錄的是**這個原型實際跑在程式碼裡的規則**，不是理想稿。
所有值都從 `index.html` 的 `:root` 與元件 CSS 抄出來，改了 code 請一併改這裡。

基準：對齊 `cmoney.tw/careers` 的實測值。
**新增任何色值或字級之前，先確認官網有沒有對應的值。**

---

## 1. 色票（全站僅這 11 色）

| Token | 值 | 用途 |
|---|---|---|
| `--red` | `#e21e28` | 主色：紅字小標、按鈕、框線、強調 |
| `--red-hover` | `#a0151c` | 主色 hover |
| `--red-active` | `#811117` | 主色 active |
| `--red-300` | `#f39ea3` | **深底上的紅字專用**、淺紅邊框 |
| `--ink` / `--text-900` | `#262626` | 深色面唯一值（Header／Footer／深色區塊）、主要文字 |
| `--text-600` | `#595959` | 次要文字 |
| `--text-400` | `#959595` | 弱文字 |
| `--text-300` | `#bfbfbf` | 更弱文字／深底次要文字 |
| `--line-200` | `#d9d9d9` | 邊框、分隔線 |
| `--bg-soft` | `#f6f6f6` | **官網唯一的淺灰底** |
| `--bg` | `#ffffff` | 白底 |

### 顏色鐵則

1. **深底上的紅字一律用 `--red-300`。**
   `#e21e28` 在 `#262626` 上對比只有 3.21:1，不過 WCAG AA。
2. **不要新增第二階淺灰。** `#f6f6f6` 是唯一的淺灰底。
   曾經試過 `#f3efe9` 暖灰，因為官網沒有這個值而改回。
3. **淺灰底上的卡片不要再用 `#f6f6f6`**，會和底色同色而消失。
   用白底 + `1px solid var(--line-200)`（`.cmp-field` 就是這樣處理的）。

---

## 2. 字級（9 階，固定 px，不使用 clamp）

```
52/64 · 40/48 · 30/36 · 24/32 · 20/32 · 20/28 · 18/28 · 18/24 · 16/24 · 14/20 · 12/16
```

- **字重僅 400 / 500 / 600。**
- `letter-spacing` 一律 `--ls` = `.004em`；uppercase 標籤用 `--ls-caps` = `.08em`。
- **字體**：系統字堆疊，不載入 webfont。
  `-apple-system, system-ui, "Microsoft JhengHei", "Noto Sans", "Helvetica Neue", Helvetica, Arial, sans-serif`

### 行動版（`max-width:768px`）

H1 52→30 · H2 30→24 · H3 24→20 · desc 20→18

### 刻意破格（CSS 已加註解）

| 例外 | 值 | 理由 |
|---|---|---|
| `.hero__title` | `clamp(44px,6.4vw,92px)` / 700 / `-.075em` | 保留原視覺重量 |
| Display 層 `.h2` `.statement` `.northstar__title` | 52/64，≤1200 降 40/48，≤768 降 30/36 | 借官網 H1 的階給章節主標，拉開與段內小標的層級 |
| `.founder__title` | 44/60 / 700，紅字 900 | Figma node 6727:13541 稿上值，不在 9 階與 400–500–600 內；≤1200 降 40/48、≤768 降 30/36 |
| `.founder__kicker` | 16/24 / 500 / `letter-spacing:2px` | 同上，比共用 `.eyebrow`（14/20、`--text-300`）重一階 |

**Display 層是全站唯一允許超過 30/36 的標題群組，新增標題請勿沿用。**
**`.founder__*` 的破格只限創辦人區塊，是照 Figma 實作的結果，不要擴散到其他段落。**

---

## 3. 間距與版面

| Token | 值 | 說明 |
|---|---|---|
| `--container` | `1152px` | 對齊官網 `.container` |
| `--section-pad` | `120px`（≤1200 降 96） | 換底色／換主題的段間距 |
| `--section-pad-tight` | `80px`（≤1200 降 64） | 同底色、同一組敘事的相鄰邊 |
| `--block-gap` | `72px`（≤1200 降 56） | 段內大區塊間距 |

**`--block-gap` 必須恆小於 `--section-pad-tight`**，層級才立得住。

### 斷點

`1200` · `980` · `768`（行動版主斷點）· `640`

### 圓角

卡片 `16px` 為主，小卡 `12px`，大區塊 `20px`／`24px`，膠囊 `100px`／`999px`。
新增元件請從既有值挑，不要再開新階。

---

## 4. 動態

- 緩動一律 `--ease` = `cubic-bezier(.22,.61,.36,1)`。
- 一般過場 `.25s`～`.3s`；展開／收合 `.4s`。
- **必須支援 `prefers-reduced-motion:reduce`**：CSS 有對應區塊，JS 也用
  `matchMedia` 判斷後跳過動畫。改互動時要一起測。

---

## 5. 元件規則

### 5.0 按鈕 `.btn`（全站唯一一組）

原本 `.btn` / `.btn-pill` / `.btn-link` 三套並存、值各自抄一份，改一個地方會漏掉另外兩個，
已整併成單一元件。**新增按鈕一律用 `.btn` + 變體，不要再開第三套。**

基準（對齊官網 careers）：高 `40`、圓角 `100px`、`16/24 · 600`、icon `20`、`gap 4`、
`border:1px solid transparent`（所有變體邊框都佔位，換變體不會位移）。

| 變體 | 用在哪 | 樣式 |
| --- | --- | --- |
| `--solid` | 主要動作，任何底 | `--red` 實心白字；`:active` 收深 `--red-active` |
| `--ghost` | 次要動作，**壓在照片／深底上** | `rgba(0,0,0,.1)` + `blur(25px)` + `1px --line-200` + 白字；`:active` 純黑 |
| `--line` | 次要動作，**站在白底／淺灰底上** | 透明底 + `1px --red` + 紅字；hover 填紅 |

- `--ghost` 照官網 `careers/aboutus` 的白色線框鈕實測值做，官網只定義 `:active`，
  hover 是本站補的，加深方向與 `:active` 一致。左右 padding 官網是 `35/37px`，
  本站按鈕多為並排，維持 `16px` 以免寬度失衡。
- **白底不要用 `--ghost`**：半透明黑底沒有照片襯底會糊掉，白字直接看不見。
- icon 一律掛 `.btn__icon`（`<span>` 文字箭頭或 `<svg>` 都可），
  hover 預設向右位移；`--down` / `--up` 修飾詞改成向下／向上。

### 5.1 小標（kicker）

`.cmp__kicker` — `14px / 600 / line-height 20px / --ls-caps / var(--red)`，前面帶紅色圓形 icon。
**同一個展開區塊裡的所有小標都吃這一級**，包含「兩種做法，你會選哪一個？」(`.cmp__caselabel`，無 icon)。

### 5.2 事業群卡片 `.bgc`

- 卡高三排遞減：通欄 `480` ／ 中排 `420` ／ 下排 `360`。
- **五張卡的標題 `.bgc__name` 統一 30/36**（≤768px 統一 20/28）。
  層級由卡片尺寸與留白表達，不再用字級遞減。

### 5.3 工作現場文章卡 `.cmp-field`

- 白底 + `1px solid var(--line-200)`，圓角 16。
- **整張卡可點**：用 stretched link（`.cmp-field__link::after{inset:0}` + 卡片
  `position:relative`），不加 JS、不包 `<a>`。
- Hover：卡片框線轉紅 + 淡陰影，膠囊按鈕同步填紅。
- 鍵盤焦點：`@supports selector(:has(*))` 時把焦點框畫在整張卡上，
  不支援 `:has()` 的瀏覽器保留膠囊原本的焦點框。

### 5.4 二選一情境互動 `.cmp__case`

版面順序固定：

```
小標（紅）
└ 灰底框 .cmp__casebox
   ├ 情境題幹 .cmp__caseq
   ├ A / B 兩張選項卡 .cmp__opt（B 帶 .cmp__opt--ours）
   └ 作答後展開 .cmp__reveal
      ├ A/B 價值軸兩行對照 .cmp__verdict
      ├ 紅線段：我們期待哪一種＋代價 .cmp__lead
      └ 延伸提問（粗體）.cmp__ask ← 展開區塊收在這裡
```

- **A 選項不掛任何徽章**，只有 B 顯示「更接近 CMoney 的做法」，且作答後才淡入。
- `.cmp__verdict` 兩行各限 **1 行**（約 27 字內），超過就換行、對照感會散掉。
- `.cmp__lead` 控制在 **3–4 行**。
- **展開區塊以延伸提問作結**，不再附面試提示句；面試確認項已經在同一列右欄的
  「面試時，建議你確認：…」出現過，放兩次會稀釋問句的收尾力道。
- `.cmp__caseq`（情境題幹）與 `.cmp__ask`（延伸提問）、右欄的面試確認項
  同為 **16/24 · 600 · `--text-900`**，三者是同一層問句。

### 5.5 創辦人區塊 `.founder`

照 Figma node 6727:13541（variation-a-editorial，1440×920）實作，是**全站唯一不套
`.container` 的段落**：左欄 640px 人像出血到瀏覽器邊緣，右欄 800px 內容，
內距 `100 / 100 / 100 / 80`，三個區塊（標題、兩欄內文、頁腳）間距 48。

- 底色是白，不是 `--bg-soft`。
- 內文兩欄各 290（`1fr` + gap 40）：左欄三段 16/28，右欄引號字符 + 引文 18/32。
- 頁腳 `border-top` + `padding-top:24`，左署名右膠囊按鈕（`.btn .btn--line`，白底上的次要動作）。
- 斷點：≤1200 人像改 42% 寬；≤980 人像改 460px 橫幅置頂（`object-position:center 30%`
  才框得住臉）；≤640 內文兩欄才收成直排。**設計稿只有 1440 桌機版，以下皆為原型自行延伸。**

### 5.6 深色區塊

深色面只用 `#262626` 一個值。深底上的紅字換 `--red-300`，次要文字用
`--on-dark-2` / `--on-dark-3`。

---

## 6. 內容規則：二選一情境題

> 這一節是文案規範，不是視覺規範。改題目時照這個結構寫。

### 出題

1. **每一題錨在它所屬那一列的預設對比上**，不能飄到別的軸：
   問題先被定義 ／ 實驗換取資源 ／ 績效看問題是否前進 ／ 權責跟著承擔。
2. 情境必須是 **PM／營運／工程都可能遇到**的權衡，不要寫成單一職能的專業題。
3. **A 和 B 字數相近。** 較長的那張卡會被讀成「比較用心的答案」，鑑別力會失真。
4. 選項本文**不得出現價值提示詞**（真正的問題、證據、說清楚⋯），
   也不要把「CMoney 期待」寫進選項。
5. **A 必須是成熟工作者真的會做的選擇**，不能是稻草人。

### 解析（`.cmp__verdict` + `.cmp__lead`）

固定三段：

1. `選 A` — 對**○○**負責：這個選擇在這個情境代表什麼。
2. `選 B` — 對**○○**負責：同上。
3. 紅線段 — CMoney 期待哪一種＋公司原則的依據＋**代價是什麼**，
   結尾接「比起你選哪一個，我們更想聽你實際做過什麼：」。

現行四題的責任軸：**任務／結果** · **論證／證據** · **承諾／目標** · **流程／現場**。
四組不重複，也不互相搶。

### 延伸提問

固定三問：① 你怎麼發現／怎麼判斷 ② 你實際放掉或換掉了什麼、跟誰確認
③ **那個問題後來有沒有前進**。

第三問一律問結果，不要問流程細節（例如「技術債後來是誰還的」就是壞問題）。

### 結尾

延伸提問就是結尾，後面不要再補句子。
把「面試時可以確認什麼」留在同一列右欄的既有那行，不在情境題裡重複。

---

## 7. 改動時的檢查清單

- [ ] 沒有新增色票外的顏色、9 階外的字級
- [ ] 深底紅字用 `--red-300`
- [ ] 淺灰底上的卡片不是 `#f6f6f6`
- [ ] `--block-gap` 仍小於 `--section-pad-tight`
- [ ] 768 / 640 兩個斷點都看過
- [ ] `prefers-reduced-motion` 下互動仍可用
- [ ] 沒有破壞 `assets/` 的相對路徑，也沒有把圖片轉成 data URI
- [ ] `#values-stack` 的三張 `.vstack` 仍在同一個 section 裡（拆開 sticky 堆疊就失效）
