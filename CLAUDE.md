# CMoney 形象站 prototype

靜態原型，兩個單檔頁面，CSS 與 JS 內嵌在各自的 HTML 裡，沒有 build step：

- `index.html` — 首頁
- `business-groups.html` — 事業群詳細頁（`#g01`–`#g05`）
- `assets/` — 扁平目錄，icon 命名 `icon-{事業群}-{產品}.png`，照片 `field-*.jpg`

## 判斷與取捨

**先做再解釋，不要停下來問。** 遇到下列情況自行判斷後繼續做，做完在 commit
內文與回覆裡講清楚取捨了什麼、犧牲了什麼，讓我能決定要不要退回：

- 設計稿與現況衝突（排不下、會蓋住、與既有元件不一致）——選一個做，說明理由。
- 設計稿的數值套到 repo 的素材上失準（裁切不同等）——以構圖相符為準，註明實測。

例外：**只有整段區塊要不要留、文案要不要寫，這兩件事一定先問。**

## 對齊設計稿

- 改版需求會附 Figma node id。**預設是照 frame 重排結構**——容器層級、欄位
  歸屬、跨欄或不跨欄都要跟設計稿一致，不是只換文案與字級。若只要換文案，
  我會明講。
- 對齊後把關鍵尺寸的實測值與設計稿並列，誤差控制在 3px 內。

## 樣式紀律

- **色票以 `index.html` 為單一來源。** 需要新顏色時先在首頁那套 token 找；
  找不到就提出來討論，不要在事業群頁自行新增色值。
- 兩頁目前是兩套 token 命名，**不要互相引用或混用**：
  - `index.html`：`--text-*` / `--on-dark-*` / `--bg*` / `--red*`，單一淺底，無主題切換
  - `business-groups.html`：`--ground` / `--raise` / `--tx-1..3` / `--rule` /
    `--accent` / `--card*`，深底為預設，另有淺底對照（`data-theme` ＋
    `prefers-color-scheme`）
  - 共用：容器 1220px、`--ease:cubic-bezier(.22,.61,.36,1)`
- 已知技術債：事業群頁 12 個色值中 7 個與首頁相同（`#262626` `#595959`
  `#959595` `#bfbfbf` `#e21e28` `#f0332c` `#ffffff`），只是名字不同；另外 5 個
  是首頁沒有的（`#767676` `#d9d9d9` `#f0f0f0` `#f39ea3` `#fce9ea`）。改到相關
  區塊時往首頁色階收斂，不要再擴大差距。
- 不寫死色碼，一律走 token；要在兩個主題各取不同值時改 token 的主題定義，
  不要另寫一套深色樣式。
- 字體堆疊兩頁必須相同：`"Inter","Noto Sans TC","PingFang TC","Microsoft JhengHei",system-ui,sans-serif`。
  大數字與年份用 Inter ＋ `tabular-nums`，不要用 Roboto Mono；Roboto Mono 只
  用於 kicker、編號、日期這類小標籤。
- 新區塊優先沿用既有元件（數據卡用 `.stat`、導覽用 site-header），不要另做
  一套視覺相同的結構。

## 無障礙

- 正文與說明文字對比至少 4.5:1（WCAG AA），大字 3:1。深底與淺底各自檢查。
- 互動元素保留 focus 樣式；裝飾性 SVG 加 `aria-hidden="true"`。
- 捲動綁定的動畫一律提供 `prefers-reduced-motion:reduce` 的退場版本。

## 完成定義

改完視覺或版面，push 前在 Chromium 量測並把結果寫進 commit：

1. 寬度 **1440 / 768 / 390** 各看一次，另外**看該區塊最窄的那個容器**（多欄
   卡片要挑最窄那張），確認文字不被壓到、不與圖層重疊。
2. 列出對設計稿的關鍵尺寸實測值與誤差。
3. 確認無橫向溢出、無 console 錯誤、互動（展開收合、hover、toast）正常。

## 內容與素材

- **設計稿沒定義的文案不要自行補寫**，只放已定義的部分。
- 未定義的連結維持 `href="#"` 並加 `data-undefined="原因"` 與同文字的 `title`，
  這是既有慣例，不要改用其他寫法。
- 素材缺件時在 HTML 留 `<!-- 待補：… -->` 註記位置與接法，不要用替代內容
  填版面充數。
- 照片統一轉 JPEG（q=0.86），輸出 2 倍於顯示尺寸；icon 由 Figma 4x 匯出。

## 移除功能

移除區塊時一併清掉孤立的 CSS、JS 與 `assets/` 素材，並確認導覽列沒有留下
死錨點。

## 環境限制

網路政策擋掉 figma.com，無法匯出 SVG 或圖片資產。需要新素材時沿用站上既有
檔案，並在 commit 註明「已知落差」，不要自行重畫。

## Commit

- 訊息用繁體中文。標題一句話講改了什麼，內文說明**為什麼這樣取捨**、量測
  驗證結果、以及尚未解決的落差。
- 一次 commit 一件事。順手修的問題另外開一段說明，不要混進標題。
